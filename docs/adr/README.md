# Architecture decision records

ADRs live here as `NNNN-slug.md`. Format is in `.agents/skills/domain-modeling/ADR-FORMAT.md`.

Write one when **all three** hold: hard to reverse, surprising without context, real trade-off. A rename is a commit message, not an ADR.
